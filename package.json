{
  "name": "micropress-plugin-sample-gallery",
  "version": "0.0.4",
  "private": true,
  "scripts": {
    "build": "node scripts/bundle.js",
    "build:zip": "npm run build && cd dist && zip -r ../plugin.zip .",
    "clean": "rm -rf dist plugin.zip",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@micropress/plugin-sdk": "^0.1.3",
    "@tiptap/core": "^2.27.1",
    "@types/node": "^24.10.4",
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
